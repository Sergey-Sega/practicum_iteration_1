import { createIdArr } from '../../utils';

const contentMenuItems = ['Парковка', 'Страховка', 'Бензин', 'Обслуживание'];
const contentSocial = ['telegram', 'facebook', 'instagram'];

export const handleContentMenuItems = createIdArr(contentMenuItems);
export const handleContentSocial = createIdArr(contentSocial);
